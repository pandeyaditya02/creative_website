# Architecture

> Auto-generated by /map

## Overview

A modern, highly interactive, one-page portfolio or creative agency website built with Next.js, React, and TailwindCSS. It heavily leverages GSAP, Framer Motion, and Lenis for cinematic animations, scroll-triggered effects, and smooth scrolling experiences. The site uses a component-based architecture where various sections are composed linearly on the main page.

```
┌─────────────────────────────────────────┐
│     src/app/page.tsx (Entry Point)      │
├─────────────────────────────────────────┤
│         [UI Components Layer]           │
│   (Hero, About, Services, Portfolio)    │
├─────────────────────────────────────────┤
│           [Animation Layer]             │
│   (GSAP, Framer Motion, Lenis Scroll)   │
└─────────────────────────────────────────┘
```

## Components

The architecture is entirely frontend and consists of feature sections and utility components.

### Feature Sections
- **HeroVideo:** Introduction video or hero banner with prominent visual impact.
- **AboutSection:** Details about the agency or portfolio owner.
- **ServicesSection:** Highlights the core offerings.
- **WhyChooseUs:** Value proposition section.
- **PortfolioSection / FeaturedSection:** Showcases project work.
- **ClientsMarquee / ClienteleMarquee:** Scrolling banners for client logos.
- **StatsCounter:** Animated statistics.
- **PinnedSection:** Content that pins or sticks during scroll.
- **GrandCTA:** Primary call-to-action area.
- **ContactSection:** End-of-page contact form or details.

### Navigation & Utilities
- **Navbar:** The site’s main navigation header.
- **Preloader:** Initial loading sequence.
- **CustomCursor:** Custom interactive mouse cursor following pointer movements.
- **SmoothScroll:** Wraps the application with Lenis for fluid scrolling mechanics.

## Data Flow

As a static/client-side focused application, the data flow is relatively simple:
1. User accesses the main route (`/`).
2. `src/app/page.tsx` renders the sequence of components.
3. `Preloader` runs first, revealing the `HeroVideo`.
4. As the user scrolls (mediated by `Lenis` via `SmoothScroll`), GSAP ScrollTrigger and Framer Motion orchestrate animations for revealing and orchestrating elements like `StatsCounter` and `PinnedSection`.
5. No complex external state management is observed; state is managed locally within React components.

## Integration Points

No external API integrations (e.g., Stripe, SendGrid) or robust database connections (e.g., PostgreSQL, MongoDB) were detected. The app primarily relies on local state and assets.

## Technical Debt

- [ ] Currently, there are no test directories or evident testing frameworks in use.
- [ ] No explicit error boundary setups or advanced exception handling logic apparent in `src/app`.
- [ ] `ClientsMarquee` and `ClienteleMarquee` suggest potential duplication of the marquee logic that could be consolidated.

## Conventions

**Naming:** PascalCase for React components (`Navbar.tsx`, `HeroVideo.tsx`).
**Structure:** Flat component structure inside `src/components`, standard Next.js App Router setup under `src/app`.
**Styling:** Utility-first via TailwindCSS, bundled via Next.js and PostCSS.
**Animations:** Widespread use of `gsap` (with `@gsap/react`) and `framer-motion` for complex UI choreographies.

---
phase: 12
plan: 1
wave: 1
---

# Plan 12.1: Stitch MCP Brutalist Hero Redesign (YouTube Edition)

## Objective
The user has reverted the background video system in `HeroVideo.tsx` to use the robust YouTube IFrame API. Now, we must re-apply the ultra-premium brutalist cinematic layout (100vw/100vh constraint, massive sans-serif text anchored bottom-left, feathered gradient, custom sound toggle) by **generating the UI using the Stitch MCP** and merging it with the existing YouTube logic.

## Context
- `.gsd/ROADMAP.md`
- `src/components/HeroVideo.tsx` (Contains the restored YouTube API logic)
- Stitch Project ID: `6886874572271062189`

## Tasks

<task type="auto">
  <name>Generate Brutalist Hero UI with Stitch MCP</name>
  <files>N/A</files>
  <action>
    Call the `mcp_StitchMCP_generate_screen_from_text` tool using Project ID `6886874572271062189`.
    Prompt: "A dark premium cinematic hero overlay. 100vw and 100vh relative container with overflow-hidden and bg-black. A bottom-anchored feathered gradient (black to transparent covering bottom 60vh). Bottom-left aligned 3-tier text block: an overline 'SHOWREEL 2026' with tracking-[0.4em] text-[#F67963], a massive brutalist headline 'CRAFTING STORIES<br/>THAT MOVE' in font-sans font-black uppercase tracking-tighter leading-[0.9], and a minimal white/70 subtext. In the bottom-right corner, a sleek minimalistic 'SOUND ON/OFF' toggle button with simple expanding bars. DO NOT include an actual video element, just leave a placeholder background div where the video would go."
  </action>
  <verify>Stitch MCP generates a Screen ID and returns the component code.</verify>
  <done>Generated UI matches the brutalist aesthetic specification.</done>
</task>

<task type="auto">
  <name>Merge Stitch UI with YouTube Video Player</name>
  <files>src/components/HeroVideo.tsx</files>
  <action>
    MODIFY: src/components/HeroVideo.tsx
    - Retain ALL state hooks (`muted`, `progress`), `playerRef`, `progressIntervalRef`, `useEffect` YouTube initialization logic, and `toggleSound` logic.
    - Replace the JSX `return( ... )` block with the UI generated by Stitch MCP.
    - Carefully re-insert the YouTube video container (`<div ref={videoRef}><div id="hero-video-player" ... /></div>`) as the very first absolute layer at `z-0` behind the Stitch-generated gradient and text.
    - Wire the Stitch-generated sound toggle's `onClick={toggleSound}` and bind its visual state to the `muted` variable.
    - Ensure the progress bar's width is bound to `style={{ width: \`\${progress}%\` }}`.
    - Maintain the GSAP Parallax `videoRef` and `titleRef` animations from before.
  </action>
  <verify>Check localhost:3000 to ensure the YouTube player works seamlessly behind the new Stitch brutalist UI.</verify>
  <done>Hero section is 100vh full-bleed, YouTube player runs natively in the background, text is bottom-left brutalist, and the sound toggle accurately interacts with the YT API.</done>
</task>

## Success Criteria
- [ ] Stitch MCP successfully invoked and component generated.
- [ ] YouTube IFrame API logic fully intact and working.
- [ ] Brutalist typography and dark gradients flawlessly merged.
- [ ] No layout bleed or horizontal scrolling.

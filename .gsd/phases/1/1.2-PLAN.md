---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Core Info Sections

## Objective
Refactor the About, Services, and Stats components to ensure multi-column grids convert to vertical stacks on mobile, and massive text correctly scales down.

## Context
- .gsd/SPEC.md
- src/components/AboutSection.tsx
- src/components/ServicesSection.tsx
- src/components/StatsCounter.tsx

## Tasks

<task type="auto">
  <name>Refactor AboutSection.tsx</name>
  <files>src/components/AboutSection.tsx</files>
  <action>
    - Scale down typography.
    - Adjust whitespace (`py-24` -> `py-16 md:py-24`).
    - Stack side-by-side elements on mobile if applicable.
    - DO NOT alter DOM wrappers needed for GSAP.
  </action>
  <verify>npm run build</verify>
  <done>About section text and spacing are mobile-friendly.</done>
</task>

<task type="auto">
  <name>Refactor ServicesSection.tsx</name>
  <files>src/components/ServicesSection.tsx</files>
  <action>
    - Convert services grid (e.g. `grid-cols-2` or `grid-cols-3`) to `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` or similar.
    - Scale typography of service titles.
    - Adjust padding to eliminate dead space on mobile.
  </action>
  <verify>npm run build</verify>
  <done>Services stack vertically on mobile screens.</done>
</task>

<task type="auto">
  <name>Refactor StatsCounter.tsx</name>
  <files>src/components/StatsCounter.tsx</files>
  <action>
    - Convert stats layout from row/grid to `flex-col` or `grid-cols-1 md:grid-cols-3`.
    - Scale numeric text down for mobile to avoid overflow.
  </action>
  <verify>npm run build</verify>
  <done>Stats are readable and stacked on mobile.</done>
</task>

## Success Criteria
- [ ] About, Services, and Stats components are fully responsive.
- [ ] Build succeeds.

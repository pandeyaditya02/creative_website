---
phase: 1
plan: 4
wave: 2
---

# Plan 1.4: Social Proof & Contact

## Objective
Refactor the marquees, pinned layout, contact form, and CTA sections to scale gracefully on mobile screens.

## Context
- .gsd/SPEC.md
- src/components/ClientsMarquee.tsx
- src/components/ClienteleMarquee.tsx
- src/components/PinnedSection.tsx
- src/components/ContactSection.tsx
- src/components/GrandCTA.tsx

## Tasks

<task type="auto">
  <name>Refactor Marquees</name>
  <files>
    src/components/ClientsMarquee.tsx
    src/components/ClienteleMarquee.tsx
  </files>
  <action>
    - Ensure marquee containers don't break viewport width (use `w-full overflow-hidden`).
    - Adjust logo sizing (e.g. `h-8 md:h-12`).
  </action>
  <verify>npm run build</verify>
  <done>Marquees scroll smoothly without causing layout shifts on mobile.</done>
</task>

<task type="auto">
  <name>Refactor PinnedSection.tsx</name>
  <files>src/components/PinnedSection.tsx</files>
  <action>
    - Stack pinned content if it uses a multi-column side-by-side layout (e.g., text on left, image pinning on right) into a single vertical scrolling layout for mobile, while keeping the desktop pinning behavior intact for `lg:` breakpoints.
  </action>
  <verify>npm run build</verify>
  <done>Pinned section is readable on mobile (stacking) and functions normally on desktop.</done>
</task>

<task type="auto">
  <name>Refactor ContactSection.tsx</name>
  <files>src/components/ContactSection.tsx</files>
  <action>
    - Convert side-by-side contact layout to a single column vertical stack on mobile.
    - Ensure all form inputs (`input`, `textarea`, `button`) have at least `h-12` or `min-h-[48px]` touch targets.
  </action>
  <verify>npm run build</verify>
  <done>Contact form is usable on mobile phones with easy tapping.</done>
</task>

<task type="auto">
  <name>Refactor GrandCTA.tsx</name>
  <files>src/components/GrandCTA.tsx</files>
  <action>
    - Ensure giant CTA text scales down so it fits `w-full` on mobile.
    - Adjust padding to prevent excessive scrolling on mobile.
  </action>
  <verify>npm run build</verify>
  <done>Grand CTA looks appropriately massive but fits the mobile screen.</done>
</task>

## Success Criteria
- [ ] All social proof and contact sections are fully responsive.
- [ ] Touch targets on forms are 48px+.
- [ ] Build succeeds.

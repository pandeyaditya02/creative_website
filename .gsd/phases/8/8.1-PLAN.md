---
phase: 8
plan: 1
wave: 1
depends_on: []
files_modified:
  - src/components/Navbar.tsx
  - src/components/HeroVideo.tsx
  - src/components/ClienteleMarquee.tsx
  - src/app/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Shared utilities are extracted to prevent duplication"
    - "HeroVideo performance is improved via optimized polling"
    - "Navbar code is more maintainable"
  artifacts:
    - "src/hooks/useMediaQuery.ts (NEW)"
    - "src/hooks/usePrefersReducedMotion.ts (NEW)"
---

# Plan 8.1: Shared Utilities and Component Cleanup

<objective>
Refactor shared logic into custom hooks and cleanup component code to improve maintainability and performance.
</objective>

<context>
- .gsd/ARCHITECTURE.md
- src/components/Navbar.tsx
- src/components/HeroVideo.tsx
- src/components/ClienteleMarquee.tsx
</context>

<tasks>

<task type="auto">
  <name>Extract Shared Hooks</name>
  <files>
    - src/hooks/useMediaQuery.ts
    - src/hooks/usePrefersReducedMotion.ts
  </files>
  <action>
    Create `useMediaQuery` hook for layout-dependent logic and `usePrefersReducedMotion` for accessibility.
    AVOID: Complex logic in components that can be shared.
  </action>
  <verify>Files exist and are correctly implemented.</verify>
  <done>Hooks are ready for integration.</done>
</task>

<task type="auto">
  <name>Refactor Navbar & HeroVideo</name>
  <files>
    - src/components/Navbar.tsx
    - src/components/HeroVideo.tsx
  </files>
  <action>
    Replace local `isMobile` and `prefersReducedMotion` logic with new hooks.
    Optimize `HeroVideo` YouTube polling interval and visibility handling.
    Cleanup unused styles or variables in `Navbar`.
  </action>
  <verify>Navbar and HeroVideo function correctly on mobile and desktop.</verify>
  <done>Components are cleaner and more efficient.</done>
</task>

<task type="auto">
  <name>Optimize Assets and Animations</name>
  <files>
    - src/components/ClienteleMarquee.tsx
    - src/app/page.tsx
  </files>
  <action>
    Ensure `ClienteleMarquee` uses correct image loading strategies.
    Audit `page.tsx` for unnecessary inline styles and optimize GSAP triggers.
  </action>
  <verify>Build passes and visual audit looks correct.</verify>
  <done>Performance and maintainability improved.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Smooth animations and correct mobile layout
</verification>

<success_criteria>
- [ ] All code duplication in hooks is removed.
- [ ] Performance audit shows no degradation.
- [ ] Source files are cleaner and more focused.
</success_criteria>
